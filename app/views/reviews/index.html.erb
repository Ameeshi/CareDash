<p id="notice"><%= notice %></p>

<h3>Reviews</h3>

<table class="striped">
  <thead>
    <tr>
      <th>Description</th>
      <th>Doctor</th>
    </tr>
  </thead>

  <tbody>
    <% @reviews.each do |review| %>
      <tr>
        <td><%= link_to review.description, review %></td>
        <td><%= link_to review.doctor.name, doctor_path(review.doctor) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
